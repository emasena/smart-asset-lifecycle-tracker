AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: AWS Smart Asset Lifecycle Tracker - Week 1 foundation

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, test, prod]

Globals:
  Function:
    Runtime: python3.11
    Architectures: [x86_64]
    Timeout: 15
    MemorySize: 256
    Tracing: Active
    Environment:
      Variables:
        ASSET_TABLE_NAME: !Ref AssetTable
        LOG_LEVEL: INFO

Resources:
  AssetTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub smart-asset-tracker-${Environment}
      BillingMode: PAY_PER_REQUEST
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      SSESpecification:
        SSEEnabled: true
      AttributeDefinitions:
        - AttributeName: PK
          AttributeType: S
        - AttributeName: SK
          AttributeType: S
      KeySchema:
        - AttributeName: PK
          KeyType: HASH
        - AttributeName: SK
          KeyType: RANGE
  AssetPhotoBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub ${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerEnforced
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      CorsConfiguration:
        CorsRules:
          - AllowedMethods: [GET, POST]
            AllowedOrigins:
              - https://main.dx980x98nt3d5.amplifyapp.com
              - http://localhost:5173
              - http://localhost:5180
            AllowedHeaders: ['*']
            MaxAge: 300

  UserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Sub smart-asset-tracker-${Environment}
      UsernameAttributes: [email]
      AutoVerifiedAttributes: [email]
      MfaConfiguration: OPTIONAL
      EnabledMfas: [SOFTWARE_TOKEN_MFA]
      AccountRecoverySetting:
        RecoveryMechanisms:
          - Name: verified_email
            Priority: 1
      Policies:
        PasswordPolicy:
          MinimumLength: 12
          RequireLowercase: true
          RequireUppercase: true
          RequireNumbers: true
          RequireSymbols: true
          TemporaryPasswordValidityDays: 7
      Schema:
        - Name: department
          AttributeDataType: String
          Mutable: true
          Required: false

  UserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: !Sub smart-asset-tracker-web-${Environment}
      UserPoolId: !Ref UserPool
      GenerateSecret: false
      PreventUserExistenceErrors: ENABLED
      EnableTokenRevocation: true
      AccessTokenValidity: 60
      IdTokenValidity: 60
      RefreshTokenValidity: 7
      TokenValidityUnits:
        AccessToken: minutes
        IdToken: minutes
        RefreshToken: days
      ExplicitAuthFlows:
        - ALLOW_USER_SRP_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH

  EmployeeGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Employee
      Description: View assigned assets and report problems
      UserPoolId: !Ref UserPool
      Precedence: 50

  TechnicianGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Technician
      Description: Create assets and update operational asset data
      UserPoolId: !Ref UserPool
      Precedence: 40

  ManagerGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Manager
      Description: View department assets and reports
      UserPoolId: !Ref UserPool
      Precedence: 30

  AdministratorGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Administrator
      Description: Full asset and user administration
      UserPoolId: !Ref UserPool
      Precedence: 10

  AuditorGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Auditor
      Description: Read-only audit access
      UserPoolId: !Ref UserPool
      Precedence: 20

  AssetApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: !Sub smart-asset-tracker-${Environment}
      StageName: !Ref Environment
      TracingEnabled: true
      Auth:
        DefaultAuthorizer: CognitoAuthorizer
        AddDefaultAuthorizerToCorsPreflight: false
        Authorizers:
          CognitoAuthorizer:
            UserPoolArn: !GetAtt UserPool.Arn
      Cors:
        AllowMethods: "'GET,POST,PUT,OPTIONS'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowOrigin: "'*'"
      GatewayResponses:
        UNAUTHORIZED:
          StatusCode: '401'
          ResponseParameters:
            Headers:
              Access-Control-Allow-Origin: "'*'"
          ResponseTemplates:
            application/json: '{"error":"Unauthorized","message":"Sign in to access this resource."}'
        ACCESS_DENIED:
          StatusCode: '403'
          ResponseParameters:
            Headers:
              Access-Control-Allow-Origin: "'*'"
          ResponseTemplates:
            application/json: '{"error":"Forbidden","message":"You do not have permission to perform this action."}'

  AssetApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub smart-asset-api-${Environment}
      CodeUri: ../backend/asset_api/
      Handler: app.lambda_handler
      Environment:
        Variables:
          ASSET_PHOTO_BUCKET: !Sub ${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}
      Policies:
        - Statement:
            - Sid: AssetTableReadWrite
              Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:PutItem
                - dynamodb:DeleteItem
                - dynamodb:Scan
                - dynamodb:TransactWriteItems
              Resource: !GetAtt AssetTable.Arn
            - Sid: ReadAuthorizedAssetPhotos
              Effect: Allow
              Action:
                - s3:GetObject
              Resource:
                - !Sub arn:${AWS::Partition}:s3:::${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}/pending/*
                - !Sub arn:${AWS::Partition}:s3:::${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}/assets/*
      Events:
        CreateAsset:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /assets
            Method: POST
        ListAssets:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /assets
            Method: GET
        GetAsset:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /assets/{assetId}
            Method: GET
        GetAssetPhoto:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /assets/{assetId}/photo
            Method: GET
        UpdateAsset:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /assets/{assetId}
            Method: PUT

  PhotoUploadFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub smart-asset-photo-upload-${Environment}
      CodeUri: ../backend/asset_api/
      Handler: photo_upload.lambda_handler
      Environment:
        Variables:
          ASSET_PHOTO_BUCKET: !Ref AssetPhotoBucket
      Policies:
        - Statement:
            - Sid: WritePendingPhotosOnly
              Effect: Allow
              Action:
                - s3:PutObject
              Resource: !Sub ${AssetPhotoBucket.Arn}/pending/*
      Events:
        GetPhotoUploadForm:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /photo-uploads
            Method: POST

  PhotoAnalysisFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub smart-asset-photo-analysis-${Environment}
      CodeUri: ../backend/asset_api/
      Handler: photo_analysis.lambda_handler
      Timeout: 90
      MemorySize: 512
      Environment:
        Variables:
          ASSET_PHOTO_BUCKET: !Sub ${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}
          PHOTO_MODEL_ID: us.amazon.nova-lite-v1:0
      Policies:
        - Statement:
            - Sid: ReadPendingPhotos
              Effect: Allow
              Action:
                - s3:GetObject
              Resource: !Sub arn:${AWS::Partition}:s3:::${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}/pending/*

            - Sid: SavePhotoAnalysis
              Effect: Allow
              Action:
                - dynamodb:PutItem
                - dynamodb:UpdateItem
              Resource: !GetAtt AssetTable.Arn

            - Sid: InvokeNovaLiteProfile
              Effect: Allow
              Action:
                - bedrock:InvokeModel
              Resource: !Sub arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.amazon.nova-lite-v1:0

            - Sid: InvokeNovaLiteModels
              Effect: Allow
              Action:
                - bedrock:InvokeModel
              Resource:
                - !Sub arn:${AWS::Partition}:bedrock:us-east-1::foundation-model/amazon.nova-lite-v1:0
                - !Sub arn:${AWS::Partition}:bedrock:us-east-2::foundation-model/amazon.nova-lite-v1:0
                - !Sub arn:${AWS::Partition}:bedrock:us-west-2::foundation-model/amazon.nova-lite-v1:0
              Condition:
                StringEquals:
                  bedrock:InferenceProfileArn: !Sub arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.amazon.nova-lite-v1:0

      Events:
        NewAssetPhoto:
          Type: S3
          Properties:
            Bucket: !Ref AssetPhotoBucket
            Events: s3:ObjectCreated:Post
            Filter:
              S3Key:
                Rules:
                  - Name: prefix
                    Value: pending/

  PhotoAnalysisApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub smart-asset-photo-analysis-api-${Environment}
      CodeUri: ../backend/asset_api/
      Handler: photo_analysis.api_handler
      Environment:
        Variables:
          ASSET_PHOTO_BUCKET: !Sub ${AWS::StackName}-photos-${AWS::AccountId}-${AWS::Region}
          PHOTO_MODEL_ID: us.amazon.nova-lite-v1:0
      Policies:
        - Statement:
            - Sid: ReadPhotoAnalysis
              Effect: Allow
              Action:
                - dynamodb:GetItem
              Resource: !GetAtt AssetTable.Arn
      Events:
        GetPhotoAnalysis:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /photo-analysis
            Method: GET

  HealthFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub smart-asset-health-${Environment}
      CodeUri: ../backend/health/
      Handler: app.lambda_handler
      Policies: []
      Events:
        Health:
          Type: Api
          Properties:
            RestApiId: !Ref AssetApi
            Path: /health
            Method: GET
            Auth:
              Authorizer: NONE

Outputs:
  ApiUrl:
    Description: REST API base URL
    Value: !Sub https://${AssetApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/${Environment}
  UserPoolId:
    Value: !Ref UserPool
  UserPoolClientId:
    Value: !Ref UserPoolClient
  AssetTableName:
    Value: !Ref AssetTable
  AssetPhotoBucketName:
    Description: Private asset photograph bucket
    Value: !Ref AssetPhotoBucket
